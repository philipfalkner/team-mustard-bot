# Team Mustard Discord Bot

## Configuring the bot

Create a `secrets.json` file in the root with your bot token, like this:

```json
{
  "token": "YOUR-BOT-TOKEN"
}
```

## Running the bot

Run `npm start`
